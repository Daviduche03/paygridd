{
  "steps": {
    "build": {
      "commands": ["cd apps/dashboard-react && bun run build"]
    }
  },
  "deploy": {
    "startCommand": "cd apps/api && TZ=UTC PORT=$PORT bun run src/index.ts"
  }
}
